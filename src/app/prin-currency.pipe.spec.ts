import { PrinCurrencyPipe } from './prin-currency.pipe';

describe('PrinCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new PrinCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
